import { styled } from "styled-components";

export const SOrderList = styled.div`
   display: flex;
   gap: 10px;
   align-items: center;
   span {
    display: block;
    font-weight: bold;
   }
  
    
`
